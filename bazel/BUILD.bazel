package(default_visibility = ["//visibility:public"])

exports_files(["search.bzl"])

filegroup(
    name = "sources",
    srcs = glob(
        ["**"],
        exclude = [
            "BUILD.bazel",
            "rules_bun/**",
        ],
        allow_empty = True,
    ),
)
