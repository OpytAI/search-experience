package(default_visibility = ["//visibility:public"])

exports_files(["README.md"])

filegroup(
    name = "sources",
    srcs = glob(
        ["**"],
        exclude = ["BUILD.bazel"],
        allow_empty = True,
    ),
)
