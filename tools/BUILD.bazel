package(default_visibility = ["//visibility:public"])

exports_files([
    "bundle.mjs",
    "manifest.mjs",
])

filegroup(
    name = "sources",
    srcs = glob(
        ["**"],
        exclude = ["BUILD.bazel"],
        allow_empty = True,
    ),
)
